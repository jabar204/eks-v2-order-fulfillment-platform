apiVersion: v1
kind: Service

metadata:
  name: payment-service
  namespace: applications

spec:
  type: ClusterIP

  selector:
    app: payment-service

  ports:
    - name: http
      port: 8083
      targetPort: 8083
      protocol: TCP