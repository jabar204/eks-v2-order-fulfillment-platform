apiVersion: apps/v1
kind: Deployment

metadata:
  name: inventory-service
  namespace: applications

  labels:
    app: inventory-service
    app.kubernetes.io/name: inventory-service
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: order-fulfillment-platform

spec:
  replicas: 2

  selector:
    matchLabels:
      app: inventory-service

  template:
    metadata:
      labels:
        app: inventory-service
        app.kubernetes.io/name: inventory-service
        app.kubernetes.io/component: backend
        app.kubernetes.io/part-of: order-fulfillment-platform

    spec:
      serviceAccountName: inventory-service

      containers:
        - name: inventory-service
          image: REPLACE_WITH_ECR/inventory-service:latest
          imagePullPolicy: IfNotPresent

          ports:
            - name: http
              containerPort: 8082
              protocol: TCP

          env:
            - name: PORT
              value: "8082"

            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: inventory-service-secrets
                  key: DATABASE_URL

          readinessProbe:
            httpGet:
              path: /healthz
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /livez
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3

          resources:
            requests:
              cpu: 100m
              memory: 128Mi

            limits:
              cpu: 500m
              memory: 512Mi

      tolerations:
        - key: workload-type
          operator: Equal
          value: application
          effect: NoSchedule