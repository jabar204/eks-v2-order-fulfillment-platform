apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argocd

resources:
  - project.yaml
  - applications

labels:
  - pairs:
      app.kubernetes.io/part-of: eks-v2-platform
      app.kubernetes.io/managed-by: argocd
    includeSelectors: false
