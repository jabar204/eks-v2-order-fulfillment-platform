apiVersion: keda.sh/v1alpha1
kind: TriggerAuthentication

metadata:
  name: keda-aws
  namespace: applications

spec:
  # KEDA 2.20+: use AWS IRSA via the operator ServiceAccount role.
  podIdentity:
    provider: aws
    identityOwner: keda
