# Helm values for kedacore/keda (install during platform bootstrap).
# helm repo add kedacore https://kedacore.github.io/charts
# helm upgrade --install keda kedacore/keda -n keda --create-namespace -f values.yaml

replicaCount: 1

serviceAccount:
  create: true
  name: keda-operator
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::283434716298:role/eks-v2-dev-worker

podIdentity:
  aws:
    irsa:
      enabled: true
      roleArn: arn:aws:iam::283434716298:role/eks-v2-dev-worker

resources:
  operator:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 512Mi

prometheus:
  metricServer:
    enabled: true
  operator:
    enabled: true
